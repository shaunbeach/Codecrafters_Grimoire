import type { ModuleFile } from '../../loadModules'

const module: ModuleFile = {
  id: 'm09-objects',
  act: 'act2',
  order: 9,
  title: 'Living Things',
  region: 'The Menagerie',
  blurb: 'Data and the behaviour that belongs to it, bound into one thing.',
  concepts: ['class', '__init__', 'self', 'methods', 'inheritance', 'super()'],
  projects: ['p1-the-character-sheet', 'p2-the-base-form', 'p3-the-bestiary', 'p4-the-sealed-purse'],
}

export default module
