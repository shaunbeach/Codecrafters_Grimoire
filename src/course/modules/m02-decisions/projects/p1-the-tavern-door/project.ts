import type { ProjectFile } from '../../../../loadModules'

const project: ProjectFile = {
  slug: 'p1-the-tavern-door',
  title: 'The Tavern Door',
  tagline: 'One decision, four possible answers',
  shape: 'project',
  difficulty: 2,
  concepts: ['if / elif / else', 'order of conditions', 'not'],
  objective: 'Write check_entry(age, has_id) which decides who gets through the door.',
  stage: 'console',
  source: 'day-03',
}
export default project
