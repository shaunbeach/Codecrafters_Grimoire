import type { ProjectFile } from '../../../../loadModules'

const project: ProjectFile = {
  slug: 'p3-the-next-task',
  title: 'The Next Job',
  tagline: 'Pop the front, and number what is left',
  shape: 'project',
  difficulty: 2,
  concepts: ['pop()', 'enumerate()', 'copies'],
  objective: 'Write next_task(tasks) and pending_tasks(tasks).',
  stage: 'console',
  source: 'day-08b',
}
export default project
