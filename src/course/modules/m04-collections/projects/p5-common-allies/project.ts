import type { ProjectFile } from '../../../../loadModules'

const project: ProjectFile = {
  slug: 'p5-common-allies',
  title: 'Common Allies',
  tagline: 'What two lists have in common',
  shape: 'drill',
  difficulty: 2,
  concepts: ['in', 'membership', 'sorted()', 'duplicates'],
  objective: 'Write common_allies(a, b) returning the names that appear on both rosters.',
  stage: 'console',
}
export default project
