import type { ProjectFile } from '../../../../loadModules'

const project: ProjectFile = {
  slug: 'p4-the-cursed-count',
  title: 'The Cursed Count',
  tagline: 'Skip one thing, stop at another',
  shape: 'drill',
  difficulty: 2,
  concepts: ['continue', 'break', 'accumulators', 'for'],
  objective: 'Write tally(numbers) which sums a list, skipping the cursed and halting at the void.',
  stage: 'console',
}
export default project
