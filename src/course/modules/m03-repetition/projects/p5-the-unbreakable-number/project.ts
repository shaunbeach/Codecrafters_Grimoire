import type { ProjectFile } from '../../../../loadModules'

const project: ProjectFile = {
  slug: 'p5-the-unbreakable-number',
  title: 'The Unbreakable Number',
  tagline: 'Search for a reason to say no',
  shape: 'drill',
  difficulty: 3,
  concepts: ['for', 'range()', 'modulo', 'early return'],
  objective: 'Write is_prime(n) deciding whether a number has any divisor but itself and one.',
  stage: 'console',
}
export default project
